language: python
python:
  - "3.7.0"

services:
  -postgresql

install:
  - pip install -r requirements.txt
  - pip install coveralls

script:
  -  coverage run --source=authors/ manage.py test

after_success:
  - coveralls